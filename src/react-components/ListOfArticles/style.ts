import styled from 'styled-components';


export const ListOfArticlesStyled = styled.section`
   background-color: var(--bg-light);
   margin: 1rem 0;
   border-radius: var(--border-radius);

   article:last-child {
      border: none;
   }
`